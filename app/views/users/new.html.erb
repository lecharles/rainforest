<!-- <h1>Hey User, Sign Up</h1> -->

<%= form_for @user do |f| %>

  <% if @user.errors.any? %>
    <div class="error_messages">
      <ul>
        <li>
          Form is invalid.
          <% @user.errors.full_messages.each do |message| %>
            <%= message %>
          <% end %>.
        </li>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %><br/>
    <%= f.email_field :email %>
  </div>

  <div class="field">
    <%= f.label :password %><br/>
    <%= f.password_field :password %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br/>
    <%= f.password_field :password_confirmation %>
  </div>

  <div class="actions"><%= f.submit "sign in" %></div>

<% end %>

<!-- Questions:
Q: How to add a value of "Sign Up!" instead of "create user" when it seems we only have access to the form's div ?
R: The beauty of helper form is allowing you to enter the value of that rails version on input tag as shown in "sign in" at L31  -->
